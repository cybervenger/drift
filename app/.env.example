# Optional. Defaults to http://127.0.0.1:5173/callback for local dev.
# Set this to your deployed GitHub Pages callback URL in production
# (must also be added as a Redirect URI in the Spotify Dashboard):
# VITE_REDIRECT_URI=https://<username>.github.io/drift/callback
